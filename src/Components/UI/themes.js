import { colors } from "./variables";

export const LightTheme = {
  body: colors.background,
  inside: colors.content,
  text: colors.text,
  filter: "",
};

export const DarkTheme = {
  body: colors.backgroundDark,
  inside: colors.contentDark,
  text: colors.textDark,
  filter: "invert(100%)",
};
